{
  "name": "@openwork/server",
  "version": "0.1.0",
  "private": true,
  "description": "OpenWork server backend - file / workspace / agent / LLM / MCP REST·SSE API",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "predev": "npm --prefix ../.. run build:agent",
    "dev": "tsx watch src/run.ts",
    "build": "tsc",
    "start": "node dist/run.js"
  },
  "dependencies": {
    "@openwork/agent": "*",
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
